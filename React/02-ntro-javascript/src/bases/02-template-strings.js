//variables y constantes

const nombre = 'Dary';
const apellido = 'Canseco';

let valorDado = 5;

console.log(`Hola ${nombre} ${apellido} ${valorDado*2}`)

function saludo(){
  return `Hola ${nombre} ${apellido} ${valorDado * 2}`;
}
console.log(`Esto es de la funcion ${saludo()}`);
